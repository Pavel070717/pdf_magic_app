# Текущее состояние проекта

**Дата**: 9 сентября 2026

## Статус

Проект стабилен, все компоненты работают. CI проходит. Покрытие тестами ~77%.

## Что сделано

1. ✅ Полная переработка тестов: 209 → 356 тестов
2. ✅ Покрытие: 55% → 77%
3. ✅ Обновлён graphify: граф 919 нод, 1989 рёбер, 35 community
4. ✅ Удалён кастомный `make_obsidian.py`
5. ✅ Установлен graphify v0.9.55
6. ✅ Установлен graphify skill для OpenCode
7. ✅ Создана папка `memory/` для AI-агентов
8. ✅ Исправления 4 дефектов: двойная нумерация, общий номер PDF+Excel, полноценный сброс БД, очистка списка файлов
9. ✅ Импорт всей папки с диска: `POST /api/files/add-from-dir` (рекурсивно, natsort-порядок, источники не трогаются) + UI на страницах «Файлы» и «Директории»
10. ✅ Тесты: 379 (378 + 1 новый по гиперссылке)
11. ✅ Лint: ruff, mypy, black — чисто
12. ✅ Graphify пересобран: 1239 нод, 2358 рёбер, 77 community
13. ✅ Гиперссылки в Excel-реестре: колонка «Гиперссылка» = относительная формула `=HYPERLINK(...)` — работает при переносе папки с реестром

## Структура проекта

```
pdf_magic_app/
├── app.py                    # Точка входа
├── run.py                    # Лаунчер
├── reset_project.py          # Сброс проекта
├── state.json                # JSON-хранилище
├── data/materials.db         # SQLite
├── routes/                   # 12 модулей (11 Blueprint'ов)
├── utils/                    # 4 модуля
├── templates/                # 11 HTML + 1 XLSX
├── tests/                    # 379 тестов
├── graphify-out/             # Граф знаний
├── memory/                   # Документация для AI
└── .github/workflows/ci.yml  # CI/CD
```

## Известные ограничения

1. **Экспорт PDF в АОСР** — работает только с Microsoft Excel (win32com)
2. **Конвертер PDF** — требует Java (OpenDataLoader)
3. **Платформа** — Windows (используются Windows-специфичные вызовы)
4. **app.py** — покрытие 50% (main() и kill_port сложно тестировать)
5. **reset_project.py** — 0% (admin utility, не тестируется)

## Зависимости

### Основные
- Flask, flask-cors
- waitress (production server)
- python-dotenv
- openpyxl, pypdf
- send2trash

### Dev
- pytest, pytest-cov
- black, isort, flake8
- mypy, bandit, safety

### Опциональные
- win32com.client (экспорт PDF, только Windows)
- Java + OpenDataLoader (конвертер PDF)
- graphify (граф знаний)

## Как запускать

```bash
python run.py              # Проверка зависимостей + запуск
python app.py              # Напрямую
pytest tests/              # Тесты
graphify update .          # Обновить граф
```

## Контакты

- **Репозиторий**: https://github.com/Pavel070717/pdf_magic_app
- **CI/CD**: GitHub Actions (автоматически при push)

---
Связано: [[00-Обзор-проекта]] | [[01-Архитектура]] | [[19-Тесты]] | [[20-CI-CD]]
